{
  "title": "ComplaintSense: Consumer Complaint Classification Benchmark",
  "id": "YOUR_KAGGLE_USERNAME/complaint-classification",
  "subtitle": "Ten-way complaint routing with transformer classifiers",
  "description": "380 grouped training complaints and 160 held-out-family test rows. Predict Category per ComplaintId. Scored by Kaggle F1 Score (Balanced).",
  "licenses": [{ "name": "CC-BY-4.0" }],
  "keywords": ["classification", "bert", "nlp", "customer-support", "multi-class"],
  "resources": [
    {
      "path": "train_complaints.csv",
      "description": "380 training complaints with Category labels.",
      "schema": {
        "fields": [
          { "name": "ComplaintId", "type": "integer" },
          { "name": "text", "type": "string" },
          { "name": "Category", "type": "string" },
          { "name": "FamilyId", "type": "string" }
        ]
      }
    },
    {
      "path": "test_complaints.csv",
      "description": "160 held-out-family test complaints without labels.",
      "schema": {
        "fields": [
          { "name": "ComplaintId", "type": "integer" },
          { "name": "text", "type": "string" }
        ]
      }
    },
    {
      "path": "sample_submission.csv",
      "schema": {
        "fields": [
          { "name": "ComplaintId", "type": "integer" },
          { "name": "Category", "type": "string" }
        ]
      }
    },
    { "path": "baseline_submission.csv", "description": "TF-IDF + logistic regression baseline." },
    { "path": "DATACARD.md", "description": "Full documentation." }
  ]
}
